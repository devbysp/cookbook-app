events {
  worker_connections 1000;
}

http {
  server {
    listen 80;
    listen [::]:80;
    server_name devbysp.com;

    location /kcal-app/food {
      proxy_pass http://backend;
    }
    location /kcal-app-react {
      proxy_pass http://frontend;
    }
  }
}
