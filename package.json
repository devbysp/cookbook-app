{
  "name": "kcal-app-express",
  "version": "1.0.0",
  "description": "backend for kcal-app-react",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "docker-desktop": "rm ~/.docker/config.json && systemctl --user start docker-desktop && dockerd &> /dev/null",
    "docker-login": "./docker-login.sh",
    "docker-build": "docker build . -t devbysp/kcal-app:1.0",
    "docker-start": "docker run -d -p 3000:8080 devbysp/kcal-app:1.0"
  },
  "author": "Magyari Zoltán",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.46.0",
    "eslint-config-standard": "^17.1.0",
    "eslint-plugin-import": "^2.28.0",
    "eslint-plugin-n": "^16.0.1",
    "eslint-plugin-promise": "^6.1.1"
  }
}
